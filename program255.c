#include<stdio.h>
#include<stdlib.h> //malloc calloc sathi

 typedef struct Node
{
    int data;
    struct Node *next;
}NODE, *PNODE, **PPNODE;

int main()
{
    PNODE First = NULL;
    return 0;
}